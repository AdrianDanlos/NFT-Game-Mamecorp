using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Battlepass : MonoBehaviour
{

    /* 

    ideas for currency system

    - create prefab for items to buy
    - login system (implement later)
    - load items based on whats available (to player/seasonal etc)
    - currency trading system
    
    code implementation
    - make currency and user info static/save?
    - currency manager -- dontDestroyOnLoad object

    */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
